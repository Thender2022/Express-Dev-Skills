<%- include('../partials/header') %> 

  <h3>Investments as of <%= time %></h3>
    
      <ul>
        <% investments.forEach(function(i) { %>
          <li>
            <a href="/investments/<%= i.id %>">
              <%= i.investment %>
            </a>
           
              - 
            <%= i.done ? 'done' : 'not done' %>
          </li>
        <% }); %>
      </ul> 
<%- include('../partials/footer') %>
        
